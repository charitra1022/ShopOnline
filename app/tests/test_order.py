# from django.test import TestCase

# from app.tests.create_objects import createOrder
# from ..models import OrderPlaced

# class OrderPlacedTestCase(TestCase):
#     def test_customer_creation(self):
#         t = createOrder()
#         cost = t.quantity * t.product.discounted_price
#         self.assertTrue(isinstance(t, OrderPlaced))
#         self.assertEqual(t.__str__(), str(t.id))
#         self.assertEqual(t.total_cost, cost)
